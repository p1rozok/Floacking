/*using UnityEngine;

public class BaitInstance : MonoBehaviour
{
    public Bait baitData;

    private void Start()
    {
        SpriteRenderer spriteRenderer = GetComponent<SpriteRenderer>();
        if (spriteRenderer != null && baitData != null)
        {
            spriteRenderer.sprite = baitData.Sprite; 
        }
    }
}*/